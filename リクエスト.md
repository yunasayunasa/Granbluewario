# グラブル風ワリオクローン ゲーム 要件定義書

> このドキュメントは Claude Code に渡して実装してもらうための要件定義です。
> 不明点があれば実装を止めて質問してください。推測で進めないこと。

-----

## 1. プロジェクト概要

### 1.1 ゲームタイトル（仮）

`グラブルワリオ` （実装中に仮タイトルとして使用。最終タイトルは別途決定）

### 1.2 ジャンル

超短時間ミニゲーム連続プレイ型アクション（メイドインワリオのクローン）

### 1.3 コンセプト

- 数秒で終わる多様なミニゲームを次々にクリアしていくエンドレスモード
- スマホ縦持ちブラウザで動作
- グラブル（グランブルーファンタジー）の世界観をフレーバーとして活用したファンゲーム

### 1.4 ターゲットプラットフォーム

- **スマートフォン縦持ち**（主ターゲット）
- モダンブラウザ（Safari iOS / Chrome Android / デスクトップChrome）
- タッチ操作を第一に設計。マウスでも動作すること

### 1.5 表示言語

**日本語**（UI・テキスト・キャラ名・ミニゲーム指示すべて）

-----

## 2. 技術スタック

### 2.1 採用スタック

**バニラ TypeScript + Vite + HTML Canvas**

#### 選定理由

- スマホブラウザで軽快に動作させたい → フレームワーク依存を減らす
- ミニゲームごとに独立したゲームループと描画が必要 → Canvas が適切
- 後からミニゲームを増やしたいので型安全が欲しい → TypeScript
- 開発体験と本番ビルドの両立 → Vite
- 静的ホスティング（GitHub Pages など）で公開可能

### 2.2 依存ライブラリ

- **必須**: TypeScript, Vite のみ
- **禁止**: React, Vue などのUIフレームワーク（オーバーヘッド回避のため）
- **禁止**: ゲームエンジン（Phaser 等）の導入。Canvas APIを直接使う
- 音声は Web Audio API を直接使用

### 2.3 動作環境

- Node.js 20 以上
- パッケージマネージャは npm
- ビルド成果物は `dist/` に出力され、静的ホスティング可能であること

-----

## 3. ディレクトリ構成

以下の構成を厳守してください。ミニゲームを追加しやすい設計が最重要です。

```
project-root/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── public/
│   └── assets/
│       ├── images/        # プレースホルダー画像（後で差し替え）
│       │   ├── characters/  # キャラ立ち絵プレースホルダー
│       │   ├── items/       # アイテム画像プレースホルダー
│       │   └── ui/          # UI素材プレースホルダー
│       └── sounds/        # 効果音・BGM（生成 or フリー素材）
└── src/
    ├── main.ts              # エントリポイント
    ├── core/
    │   ├── GameManager.ts   # 全体進行（ミニゲーム切り替え、ライフ、スコア）
    │   ├── MinigameBase.ts  # ミニゲーム基底クラス/インターフェース
    │   ├── MinigameRegistry.ts # ミニゲームの登録・ランダム選出
    │   ├── InputManager.ts  # タップ/スワイプ/長押し/傾きの抽象化
    │   ├── AudioManager.ts  # BGM・効果音管理
    │   ├── Renderer.ts      # Canvas描画ユーティリティ
    │   └── types.ts         # 共通型定義
    ├── minigames/
    │   ├── index.ts         # 全ミニゲームを集約してRegistryに登録
    │   ├── 01_tap_luria/    # 例: ルリアをタップ
    │   │   ├── index.ts
    │   │   └── assets.ts
    │   ├── 02_swipe_.../
    │   └── ... （合計8〜12個）
    ├── scenes/
    │   ├── TitleScene.ts    # タイトル画面
    │   ├── PlayScene.ts     # ゲーム本編（ミニゲーム連続プレイ）
    │   ├── TransitionScene.ts # ミニゲーム間のつなぎ演出
    │   └── GameOverScene.ts # ゲームオーバー画面
    ├── ui/
    │   ├── LifeDisplay.ts
    │   ├── ScoreDisplay.ts
    │   └── InstructionBanner.ts # ミニゲーム冒頭の「〇〇せよ！」表示
    └── data/
        └── flavor.ts        # GBFキャラ名・アイテム名データ（フレーバー用）
```

-----

## 4. 画面設計

### 4.1 画面サイズ・基準解像度

- **論理解像度**: 幅 360px × 高さ 640px を基準（9:16）
- 実デバイスでは内接するよう拡大縮小（レターボックス）
- Canvas は常にこの論理サイズで描画し、CSS transform で画面にフィット
- セーフエリア（iPhoneのノッチ等）を考慮して上下に余白

### 4.2 画面一覧

1. **タイトル画面**
- ゲームタイトル
- 「スタート」ボタン（大きめ、タップしやすく）
- 操作説明リンク（簡易）
1. **プレイ画面**
- 上部: 残りライフ（ハート3個）、スコア（クリア数）
- 中央: ミニゲーム本体の描画領域
- 下部: タイマーゲージ（ミニゲームの残り時間を視覚化）
1. **ミニゲーム導入カットイン**（トランジション）
- 指示文を大きく表示（例:「タップせよ！」「こうげきを よけろ！」）
- 0.8〜1.2秒
1. **成功/失敗の短い演出**（0.5秒程度）
1. **ゲームオーバー画面**
- 最終スコア（クリアしたミニゲーム数）
- 「もう一度」ボタン
- 「タイトルへ」ボタン

### 4.3 UI デザイン方針

- プレースホルダーなので機能性優先。派手な装飾は不要
- フォントはシステムフォント or Google Fonts の日本語フォント
- ボタンは最小 44×44px のタップ領域を確保
- 色は仮のパレットでよい。CSS変数で管理し後で差し替え可能に

-----

## 5. ゲームシステム仕様

### 5.1 ゲームループ（エンドレスモード）

1. タイトル → スタートボタン
1. 以下を繰り返す:
- ミニゲームをランダム選出（直前と同じは避ける）
- 導入カットイン表示（指示文 + 短い効果音）
- ミニゲーム本編（制限時間内にクリア条件達成で成功、時間切れ or 失敗条件で失敗）
- 結果演出（成功 or 失敗）
- 成功ならスコア+1、失敗ならライフ-1
- ライフ0 → ゲームオーバー画面へ

### 5.2 難易度スケーリング（エンドレス）

- 初期制限時間: 各ミニゲーム 5秒
- スコアが上がるごとに制限時間を短縮:
  - スコア 0〜9: 5.0秒
  - スコア 10〜19: 4.5秒
  - スコア 20〜29: 4.0秒
  - スコア 30〜39: 3.5秒
  - スコア 40〜49: 3.0秒
  - スコア 50以上: 2.5秒（下限）
- ミニゲーム側も `difficulty`（0.0〜1.0）パラメータを受け取り、内部パラメータ（速度・敵の数など）を線形補間すること

### 5.3 ライフ仕様

- 初期ライフ: **3**
- ミニゲーム失敗 or 時間切れで -1
- 回復はなし（定番仕様）
- ライフ0 で即ゲームオーバー

### 5.4 データ保存

- **保存しない**。毎回タイトル画面からリセット
- localStorage / cookie は使用しない

-----

## 6. 操作仕様

### 6.1 サポートする入力

`InputManager` で以下の入力を抽象化し、ミニゲームは必要なものを宣言的に使う:

|入力種別          |説明          |実装                                                 |
|--------------|------------|---------------------------------------------------|
|**tap**       |画面を素早くタップ   |touchstart/mousedown 〜 touchend/mouseup が閾値時間/移動量以内|
|**swipe**     |上下左右にスワイプ   |touch開始〜終了の変位ベクトルで方向判定（閾値: 30px以上）                 |
|**long_press**|長押し（500ms以上）|touchstart継続時間で判定                                  |
|**tilt**      |端末の傾き       |DeviceOrientation API。iOS は許可ダイアログが必要              |

### 6.2 入力許可ダイアログ

- **傾き（tilt）** を使うミニゲームが初めて出る直前に、iOS向け許可ダイアログを出す
- タイトル画面で「高度な操作を有効にする」ボタンを置き、そこで一括許可できるようにする
- 許可されない場合、tilt を使うミニゲームは自動的にスキップ or 代替操作（スワイプなど）にフォールバック

### 6.3 操作方針

- **基本はタップとスワイプ**で成立するミニゲームを多めに
- **長押し・傾きは味付け程度**（1〜2個ずつ）
- ミニゲームの多様性を最優先

-----

## 7. ミニゲーム仕様

### 7.1 ミニゲームインターフェース

すべてのミニゲームは以下のインターフェースを実装すること:

```typescript
// src/core/MinigameBase.ts
export interface MinigameContext {
  canvas: HTMLCanvasElement;
  ctx: CanvasRenderingContext2D;
  input: InputManager;
  audio: AudioManager;
  difficulty: number; // 0.0 (最易) 〜 1.0 (最難)
  timeLimit: number;  // 秒
  onSuccess: () => void;
  onFailure: () => void;
}

export interface Minigame {
  id: string;                    // 一意ID（例: "tap_luria"）
  instruction: string;           // 画面に表示する指示文（例: "ルリアをタップ！"）
  requiredInputs: InputType[];   // このゲームが使う入力種別
  init(ctx: MinigameContext): void;  // 初期化
  update(deltaTime: number): void;   // 毎フレーム呼ばれる
  render(): void;                    // 描画
  destroy(): void;                   // クリーンアップ
}
```

### 7.2 ミニゲームの実装例（合計 10個 を目標）

以下は**例**です。Claude Code は下記をベースに実装し、バランスよく10個作成してください。
GBFフレーバーは `src/data/flavor.ts` のキャラ名・アイテム名リストから参照する形にしてください。

|# |ID               |指示文            |操作        |クリア条件                     |
|--|-----------------|---------------|----------|--------------------------|
|1 |`tap_luria`      |ルリアをタップ！       |tap       |時間内にルリアのキャラを1回タップ         |
|2 |`avoid_attack`   |こうげきを よけろ！     |swipe     |飛んでくる攻撃を2〜3回スワイプで回避       |
|3 |`catch_item`     |アイテムをキャッチ！     |tap（連打可）  |落下するアイテムを一定数タップで取得        |
|4 |`feed_vyrn`      |ビィに エサをやれ！     |tap       |ビィ（キャラ）にアイテムを連続でドラッグ or 連打|
|5 |`choose_element` |ひゃくれつけん！       |tap 連打    |制限時間内に規定回数タップ             |
|6 |`swipe_direction`|↑ スワイプ！        |swipe     |指示された方向に正しくスワイプ           |
|7 |`hold_charge`    |ためろ！           |long_press|指示時間だけ長押しをキープ（早くても遅くてもダメ） |
|8 |`tilt_balance`   |バランスを取れ！       |tilt      |端末を傾けてキャラを中央に保つ           |
|9 |`find_target`    |グラン / ジータを さがせ！|tap       |複数キャラの中から指示されたキャラをタップ     |
|10|`order_sequence` |じゅんばんに タップ！    |tap       |1→2→3 の順で数字をタップ           |

### 7.3 ミニゲーム実装ルール

- 各ミニゲームは `src/minigames/XX_id/` 配下に**完全に独立**して作成する
- 他のミニゲームの内部状態を参照しないこと
- `MinigameRegistry` には `src/minigames/index.ts` で一括登録
- **グラブルの版権画像は絶対に使用しない**。プレースホルダーは幾何図形 + テキストラベルで表現
  - 例: ルリア = 青い円 + 「ルリア」テキスト
  - 画像パスは `public/assets/images/characters/luria.png` のような想定で定数化し、現状はプレースホルダー画像（単色 + 文字）を生成 or 用意する
- 画像は `<img>` ロード失敗時にフォールバック描画する実装にしておく（後で差し替えやすい）

### 7.4 フレーバーデータ

`src/data/flavor.ts` に以下のデータを定数配列として定義:

- **キャラ名リスト**: グラン, ジータ, ルリア, ビィ, カタリナ, ラカム, イオ, オイゲン, ロゼッタ, ヴィーラ など主要キャラ15〜20名
- **アイテム名リスト**: ポーション, エーテル, ソウルパウダー, ヒヒイロカネ, 碧麗の証, 天星器 など10〜15個
- **敵・召喚獣名リスト**: バハムート, ルシフェル, グランデ, ティアマト など10個程度

これらはミニゲーム内で指示文や選択肢の生成に使う。

-----

## 8. 音声仕様

### 8.1 実装方針

- **Web Audio API** を使って効果音を**プログラム生成**する（外部音声ファイル不要）
- BGM もシンプルなループを Web Audio API の OscillatorNode で生成
- 将来的にフリー素材の mp3/ogg に差し替えられるよう、`AudioManager` は抽象化しておく

### 8.2 必要な音

|種別         |用途                  |
|-----------|--------------------|
|BGM（プレイ中）  |軽快なループ。テンポは難易度に応じて加速|
|SE: 成功     |ミニゲームクリア時           |
|SE: 失敗     |ミニゲーム失敗時            |
|SE: カットイン  |指示文表示時              |
|SE: タップ    |UIボタン押下             |
|SE: ゲームオーバー|ライフ0時               |

### 8.3 ミュート機能

- 画面右上に常時ミュートボタンを配置
- 状態はセッション中のみ保持（保存なし）

-----

## 9. 非機能要件

### 9.1 パフォーマンス

- 60fps を目標（requestAnimationFrame）
- iPhone SE 第2世代クラスのスマホで快適動作
- 画像はロード完了を待ってからゲーム開始（ローディング画面を最低1つ用意）

### 9.2 ブラウザ互換

- iOS Safari 15+
- Android Chrome 最新版
- デスクトップ Chrome / Firefox / Safari 最新版

### 9.3 アクセシビリティ

- キーボード操作は必須ではないが、デスクトップで遊ぶ人向けにマウスクリックで tap 代替ができるようにする
- スペースキーでタップ代替ができると親切（任意）

### 9.4 コード品質

- TypeScript の `strict: true`
- ESLint は導入不要（シンプルさ優先）。ただしコードは整形済みで提出
- 各ファイル冒頭に簡潔なコメントでファイルの役割を記載
- 公開メソッドにはJSDocコメント

-----

## 10. 納品物 / 成果物

### 10.1 Claude Code が実装するもの

1. 上記ディレクトリ構成に従ったソースコード一式
1. `npm install && npm run dev` で起動できる状態
1. `npm run build` で `dist/` に静的ファイル出力される状態
1. **ミニゲーム 10個** が動作する状態（7.2の表をベースに、実装時にバランス調整可）
1. プレースホルダー画像は最低限用意（キャラ1体につき1枚の単色円+名前入り画像）
1. README.md に以下を記載:
- 起動手順
- ミニゲーム追加方法（`MinigameBase` を実装して `src/minigames/index.ts` に登録）
- 画像差し替え方法

### 10.2 実装の進め方（推奨順序）

1. プロジェクト初期化（Vite + TypeScript）
1. `core/` のインフラ整備（GameManager, InputManager, AudioManager, MinigameBase）
1. シーン遷移の仕組み（Title → Play → GameOver）
1. ミニゲームを**まず1個**作って全体フローを通す（`tap_luria` 推奨）
1. 残り9個のミニゲームを段階的に追加
1. 難易度スケーリング・演出・音の調整
1. README 作成

### 10.3 段階確認

各フェーズ完了時、動作確認できる状態でコミット推奨。
Claude Code が実装中に迷ったら、推測せず質問すること。

-----

## 11. 明示的に「やらないこと」

混乱防止のため、やらないことを列挙:

- ❌ グラブル公式の画像・音楽・ロゴの使用
- ❌ バックエンド / サーバー / DB
- ❌ ユーザー登録 / ログイン
- ❌ 課金・広告
- ❌ マルチプレイ
- ❌ データ保存（localStorage含む）
- ❌ PWA化 / オフライン対応
- ❌ 多言語対応（日本語のみ）
- ❌ 横画面対応（縦持ち固定）
- ❌ React / Vue / ゲームエンジンの導入

-----

## 12. 用語集

|用語      |意味             |
|--------|---------------|
|GBF     |グランブルーファンタジー   |
|ミニゲーム   |3〜5秒で終わる1つの課題  |
|カットイン   |ミニゲーム開始前の指示表示演出|
|エンドレス   |失敗するまで無限に続くモード |
|プレースホルダー|後で差し替え前提の仮素材   |

-----

以上が要件です。不明点があれば実装開始前に質問してください。